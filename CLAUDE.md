# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

`create-domain-cortex` is a CommonJS Node.js package (entry point: `index.js`). The project is in early development — no source files, test framework, or build tooling have been configured yet.

## Commands

```bash
npm test    # Currently a stub — no test framework configured
```

No build or lint scripts are defined yet.

## Architecture

No source code exists yet. When implementing, note:

- Module format: CommonJS (`"type": "commonjs"`)
- Declared main entry: `index.js`
